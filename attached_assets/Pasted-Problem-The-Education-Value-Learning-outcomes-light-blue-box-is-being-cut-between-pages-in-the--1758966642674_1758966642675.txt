Problem: The “Education Value & Learning outcomes” light-blue box is being cut between pages in the image-based PDF. 
Fix: Before PDF generation, automatically split that big box into multiple smaller blue cards (“continued”) so breaks occur between cards, not through one box.

Do EXACTLY this:

1) Add print-safe styles to public/styles.css (append at bottom):

/* Page break marker we’ll insert between cards */
.page-break { break-before: page; page-break-before: always; height: 0; }

/* The blue card style (same look as current box) */
.outcome-card {
  background: #E6F2FF; 
  border-radius: 8px;
  padding: 10mm;
  margin: 0 0 6mm 0;
  box-sizing: border-box;
  break-inside: avoid; 
  page-break-inside: avoid;
}
.outcome-card .continued {
  margin-top: 2mm;
  font-size: 0.9em;
  opacity: 0.7;
}
@media print {
  * { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
  .outcome-card { box-decoration-break: clone; -webkit-box-decoration-break: clone; }
}

2) Create a small DOM paginator: public/js/outcomes-paginate.js

export function paginateOutcomesByCount({
  wrapperSel = '.education-outcomes',     // wrapper of the current big blue panel
  itemSel = '.outcome-item',              // each logical bullet/paragraph inside
  perCard = 6,                            // tweak to fit your layout (try 5–7)
  titleSel = '.section-title',            // optional: selector for the panel title inside wrapper
  insertContinued = true
} = {}) {
  const wrap = document.querySelector(wrapperSel);
  if (!wrap) return;

  const items = Array.from(wrap.querySelectorAll(itemSel));
  if (!items.length) return;

  // Capture/clone the existing title if present
  let titleHTML = '';
  const titleEl = wrap.querySelector(titleSel);
  if (titleEl) titleHTML = titleEl.outerHTML;

  const frag = document.createDocumentFragment();

  for (let i = 0; i < items.length; i += perCard) {
    const card = document.createElement('div');
    card.className = 'outcome-card';

    if (titleHTML) {
      card.insertAdjacentHTML('afterbegin', titleHTML);
      if (insertContinued && i > 0) {
        const cont = document.createElement('div');
        cont.className = 'continued';
        cont.textContent = '(continued)';
        card.appendChild(cont);
      }
    }

    // Make a list/container for this batch
    const list = document.createElement('div');
    list.className = 'outcome-list';
    items.slice(i, i + perCard).forEach(el => list.appendChild(el));
    card.appendChild(list);

    frag.appendChild(card);

    // Put a hard page break between cards (except after the last one)
    if (i + perCard < items.length) {
      const br = document.createElement('div');
      br.className = 'page-break';
      frag.appendChild(br);
    }
  }

  // Replace the original big panel with the paginated cards
  wrap.replaceWith(frag);
}

3) Tag the existing HTML so the script can find it:
- Ensure the current big blue panel wrapper has class="education-outcomes".
- Ensure each bullet/paragraph inside that panel has class="outcome-item".
- If there is a visible title inside the panel, give it class="section-title".

Example (for reference only, don’t duplicate content):
<section class="education-outcomes">
  <h3 class="section-title">Education Value & Learning outcomes</h3>
  <ul>
    <li class="outcome-item">Outcome 1 …</li>
    <li class="outcome-item">Outcome 2 …</li>
    ...
  </ul>
</section>

4) Call the paginator RIGHT BEFORE starting the image-based PDF generation (html2pdf/html2canvas/jsPDF).
- In the JS that triggers PDF capture (e.g., public/js/generate-pdf.js):

// at top
import { paginateOutcomesByCount } from './outcomes-paginate.js';

// right before html2pdf/html2canvas/jsPDF runs:
paginateOutcomesByCount({
  wrapperSel: '.education-outcomes',
  itemSel: '.outcome-item',
  perCard: 6,                 // adjust to 5 or 7 if a card still overflows
  titleSel: '.section-title',
  insertContinued: true
});

// then proceed with your existing image-based PDF generation

5) Do NOT change any other sections/quotes. This only restructures the outcomes box into multiple identical blue cards with page breaks between them, so no single shaded box gets cut in the exported PDF.
